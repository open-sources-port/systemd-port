project('linux-port', 'c',
  version : '0.1',
  default_options : ['warning_level=3', 'c_std=gnu11'])

# Include directory for headers
# inc = include_directories('include')

# Subprojects (modules)
subdir('src/linux')
subdir('tests/linux')
